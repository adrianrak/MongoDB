doctype html
html
    head
        title="MongoDB"
        style 
            include ./style.css
    body
        form(action = "/addtask", method = "POST")
        h1 Welcome 
        div
            label(for = "task") Task:
            input(task = "task")
        button(type='submit') Create new task
